<Window x:Class="ShiftManagement.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:ShiftManagement.App.ViewModels"
        xmlns:views="clr-namespace:ShiftManagement.App.Views"
        mc:Ignorable="d"
        Title="勤務表自動作成システム — ライフステイむなかた" Height="800" Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}">

    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:DashboardViewModel}">
            <views:DashboardView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:StaffManagementViewModel}">
            <views:StaffManagementView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:WishInputViewModel}">
            <views:WishInputView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ScheduleGenerationViewModel}">
            <views:ScheduleGenerationView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ScheduleDetailViewModel}">
            <views:ScheduleDetailView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:CaseComparisonViewModel}">
            <views:CaseComparisonView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:SettingsViewModel}">
            <views:SettingsView/>
        </DataTemplate>

        <!-- ナビゲーションボタンスタイル -->
        <Style x:Key="NavButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#E2E8F0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="18,12"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" CornerRadius="6" Margin="6,2">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1E293B"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- 左サイドバー -->
        <Border Grid.Column="0" Background="#0F172A">
            <DockPanel LastChildFill="True">
                <!-- ロゴ・アプリ名 -->
                <StackPanel DockPanel.Dock="Top" Margin="18,24,18,24">
                    <TextBlock Text="勤務表作成システム" Foreground="White" FontSize="18" FontWeight="Bold"/>
                    <TextBlock Text="介護・看護施設向け" Foreground="#94A3B8" FontSize="12" Margin="0,4,0,0"/>
                </StackPanel>

                <!-- メニュー一覧 -->
                <StackPanel>
                    <Button Style="{StaticResource NavButton}" Content="🏠  ダッシュボード" Command="{Binding NavigateCommand}" CommandParameter="Dashboard"/>
                    <Button Style="{StaticResource NavButton}" Content="👥  職員管理" Command="{Binding NavigateCommand}" CommandParameter="Staff"/>
                    <Button Style="{StaticResource NavButton}" Content="📅  勤務希望入力" Command="{Binding NavigateCommand}" CommandParameter="Wish"/>
                    <Button Style="{StaticResource NavButton}" Content="⚡  自動生成" Command="{Binding NavigateCommand}" CommandParameter="Generation"/>
                    <Button Style="{StaticResource NavButton}" Content="📋  勤務表詳細・編集" Command="{Binding NavigateCommand}" CommandParameter="Detail"/>
                    <Button Style="{StaticResource NavButton}" Content="⚖️  3案比較" Command="{Binding NavigateCommand}" CommandParameter="Comparison"/>
                    <Button Style="{StaticResource NavButton}" Content="⚙️  設定" Command="{Binding NavigateCommand}" CommandParameter="Settings"/>
                </StackPanel>

                <!-- バージョン情報（最下部） -->
                <TextBlock DockPanel.Dock="Bottom" Text="v1.0.0 (CP-SAT搭載)" Foreground="#64748B" FontSize="11" Margin="18,0,0,16" VerticalAlignment="Bottom"/>
            </DockPanel>
        </Border>

        <!-- メインエリア -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- ヘッダーバー -->
            <Border Grid.Row="0" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="24,0">
                <DockPanel LastChildFill="False">
                    <TextBlock Text="{Binding CurrentTitle}" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
                    <TextBlock DockPanel.Dock="Right" Text="{Binding StatusMessage}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="13" VerticalAlignment="Center"/>
                </DockPanel>
            </Border>

            <!-- コンテンツエリア -->
            <ContentControl Grid.Row="1" Content="{Binding CurrentViewModel}"/>
        </Grid>
    </Grid>
</Window>
