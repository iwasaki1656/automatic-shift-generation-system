<UserControl x:Class="ShiftManagement.App.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="650" d:DesignWidth="900">
    <Grid Margin="24">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- 左ペイン: 施設・制約パラメータ設定 -->
        <Border Grid.Column="0" Background="{StaticResource SurfaceBrush}" CornerRadius="8" Padding="20" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="施設・制約パラメータ設定" FontSize="16" FontWeight="Bold" Margin="0,0,0,16"/>

                    <TextBlock Text="施設名:" Margin="0,0,0,4"/>
                    <TextBox Text="{Binding FacilityName, UpdateSourceTrigger=PropertyChanged}" Padding="8,4" Margin="0,0,0,12"/>

                    <TextBlock Text="月間最低休日数（日数）:" Margin="0,0,0,4"/>
                    <TextBox Text="{Binding MinDaysOff, UpdateSourceTrigger=PropertyChanged}" Padding="8,4" Margin="0,0,0,12"/>

                    <TextBlock Text="最大連続勤務日数:" Margin="0,0,0,4"/>
                    <TextBox Text="{Binding MaxConsecutiveWorkDays, UpdateSourceTrigger=PropertyChanged}" Padding="8,4" Margin="0,0,0,12"/>

                    <TextBlock Text="勤務負担度係数設定" FontSize="14" FontWeight="SemiBold" Margin="0,12,0,8"/>

                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="120"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                        <TextBlock Text="夜勤負担度:" VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1" Text="{Binding NightDutyLoad, UpdateSourceTrigger=PropertyChanged}" Padding="6,4"/>
                    </Grid>

                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="120"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                        <TextBlock Text="早出負担度:" VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1" Text="{Binding EarlyDutyLoad, UpdateSourceTrigger=PropertyChanged}" Padding="6,4"/>
                    </Grid>

                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="120"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                        <TextBlock Text="遅出負担度:" VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1" Text="{Binding LateDutyLoad, UpdateSourceTrigger=PropertyChanged}" Padding="6,4"/>
                    </Grid>

                    <Grid Margin="0,0,0,16">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="120"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                        <TextBlock Text="日勤負担度:" VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1" Text="{Binding DayDutyLoad, UpdateSourceTrigger=PropertyChanged}" Padding="6,4"/>
                    </Grid>

                    <Button Style="{StaticResource PrimaryButton}" Content="💾 設定を保存" Command="{Binding SaveSettingsCommand}" Margin="0,0,0,16"/>
                    <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource SuccessBrush}" FontWeight="SemiBold"/>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- 右ペイン: データベースバックアップと復元 -->
        <Border Grid.Column="2" Background="{StaticResource SurfaceBrush}" CornerRadius="8" Padding="20" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <DockPanel LastChildFill="True">
                <StackPanel DockPanel.Dock="Top" Margin="0,0,0,16">
                    <DockPanel>
                        <TextBlock Text="データバックアップ・復元" FontSize="16" FontWeight="Bold" VerticalAlignment="Center"/>
                        <Button DockPanel.Dock="Right" Style="{StaticResource PrimaryButton}" Content="＋ バックアップ作成" Command="{Binding CreateBackupCommand}"/>
                    </DockPanel>
                    <TextBlock Text="定期的にバックアップが自動作成されます。任意の過去状態に復元可能です。" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,4,0,0"/>
                </StackPanel>

                <DataGrid ItemsSource="{Binding BackupList}" AutoGenerateColumns="False" IsReadOnly="True"
                          HeadersVisibility="Column" GridLinesVisibility="Horizontal" SelectionMode="Single">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ファイル名" Binding="{Binding FileName}" Width="*"/>
                        <DataGridTextColumn Header="作成日時" Binding="{Binding CreatedAt, StringFormat={}{0:yyyy/MM/dd HH:mm}}" Width="120"/>
                        <DataGridTextColumn Header="サイズ" Binding="{Binding FormattedSize}" Width="70"/>
                        <DataGridTemplateColumn Header="復元" Width="60">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="復元" Padding="4,2" FontSize="11" Style="{StaticResource SecondaryButton}"
                                            Command="{Binding DataContext.RestoreBackupCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
