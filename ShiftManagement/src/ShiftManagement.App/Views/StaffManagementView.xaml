<UserControl x:Class="ShiftManagement.App.Views.StaffManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid Margin="24">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="400"/>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- 左ペイン: 職員一覧 -->
        <Border Grid.Column="0" Background="{StaticResource SurfaceBrush}" CornerRadius="8" Padding="16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <DockPanel LastChildFill="True">
                <DockPanel DockPanel.Dock="Top" Margin="0,0,0,12">
                    <TextBlock Text="職員一覧" FontSize="16" FontWeight="Bold" VerticalAlignment="Center"/>
                    <Button DockPanel.Dock="Right" Style="{StaticResource PrimaryButton}" Content="＋ 職員追加" Command="{Binding AddStaffCommand}"/>
                </DockPanel>

                <DataGrid ItemsSource="{Binding StaffList}" SelectedItem="{Binding SelectedStaff}" AutoGenerateColumns="False" IsReadOnly="True"
                          HeadersVisibility="Column" GridLinesVisibility="Horizontal" SelectionMode="Single">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="順" Binding="{Binding DisplayOrder}" Width="40"/>
                        <DataGridTextColumn Header="職種" Binding="{Binding JobType.DisplayName}" Width="70"/>
                        <DataGridTextColumn Header="形態" Binding="{Binding EmploymentType.DisplayName}" Width="60"/>
                        <DataGridTextColumn Header="氏名" Binding="{Binding DisplayName}" Width="110"/>
                        <DataGridTextColumn Header="資格" Binding="{Binding Qualification.DisplayName}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>

        <!-- 右ペイン: 編集フォーム -->
        <Border Grid.Column="2" Background="{StaticResource SurfaceBrush}" CornerRadius="8" Padding="20" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="職員詳細・固定条件" FontSize="16" FontWeight="Bold" Margin="0,0,0,16"/>

                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="姓:" VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1" Text="{Binding EditLastName, UpdateSourceTrigger=PropertyChanged}" Padding="6,4"/>
                    </Grid>

                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="名:" VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1" Text="{Binding EditFirstName, UpdateSourceTrigger=PropertyChanged}" Padding="6,4"/>
                    </Grid>

                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="表示順:" VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1" Text="{Binding EditDisplayOrder, UpdateSourceTrigger=PropertyChanged}" Padding="6,4"/>
                    </Grid>

                    <CheckBox Content="有効（勤務表作成対象）" IsChecked="{Binding EditIsActive}" Margin="100,0,0,20"/>

                    <StackPanel Orientation="Horizontal" Margin="100,0,0,24">
                        <Button Style="{StaticResource PrimaryButton}" Content="💾 保存" Command="{Binding SaveStaffCommand}" Margin="0,0,12,0"/>
                        <Button Style="{StaticResource SecondaryButton}" Content="無効化" Command="{Binding DeactivateStaffCommand}"/>
                    </StackPanel>

                    <!-- 固定制約リスト表示 -->
                    <TextBlock Text="登録されている固定制約" FontSize="14" FontWeight="Bold" Margin="0,16,0,8"/>
                    <ItemsControl ItemsSource="{Binding SelectedStaff.PermanentConstraints}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#F1F5F9" CornerRadius="4" Padding="10,6" Margin="0,0,0,6">
                                    <StackPanel>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding Level}" FontWeight="Bold" Foreground="{StaticResource DangerBrush}" Margin="0,0,8,0"/>
                                            <TextBlock Text="{Binding Type}" FontWeight="SemiBold"/>
                                        </StackPanel>
                                        <TextBlock Text="{Binding Note}" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,2,0,0"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource SuccessBrush}" FontWeight="SemiBold" Margin="0,16,0,0"/>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
