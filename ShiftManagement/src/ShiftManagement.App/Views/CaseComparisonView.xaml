<UserControl x:Class="ShiftManagement.App.Views.CaseComparisonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ヘッダー -->
        <Border Grid.Row="0" Background="{StaticResource SurfaceBrush}" CornerRadius="8" Padding="16" Margin="0,0,0,20" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <DockPanel LastChildFill="False">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="{Binding Year, StringFormat={}{0}年}" FontWeight="Bold" FontSize="18"/>
                    <TextBlock Text="{Binding Month, StringFormat={}{0}月}" FontWeight="Bold" FontSize="18" Margin="4,0,20,0"/>
                    <TextBlock Text="生成された3案の指標を比較し、施設方針に最も適した案を正式採用します。" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                </StackPanel>

                <Button DockPanel.Dock="Right" Style="{StaticResource SecondaryButton}" Content="🔄 再読込" Command="{Binding LoadCasesCommand}"/>
            </DockPanel>
        </Border>

        <!-- 3案カード比較 -->
        <ItemsControl Grid.Row="1" ItemsSource="{Binding Cases}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="3"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Background="{StaticResource SurfaceBrush}" CornerRadius="10" Padding="20" Margin="10" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- ヘッダー・採用バッジ -->
                            <DockPanel Grid.Row="0" Margin="0,0,0,16">
                                <TextBlock Text="{Binding CaseName}" FontSize="16" FontWeight="Bold" VerticalAlignment="Center"/>
                                <Border DockPanel.Dock="Right" Background="#DCFCE7" CornerRadius="12" Padding="8,3"
                                        Visibility="{Binding IsSelected, Converter={StaticResource BoolToVis}}">
                                    <TextBlock Text="★ 採用中" FontSize="11" FontWeight="Bold" Foreground="{StaticResource SuccessBrush}"/>
                                </Border>
                            </DockPanel>

                            <!-- スコア大きく表示 -->
                            <Border Grid.Row="1" Background="#F8FAFC" CornerRadius="8" Padding="16" Margin="0,0,0,16">
                                <StackPanel HorizontalAlignment="Center">
                                    <TextBlock Text="総合評価スコア" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center"/>
                                    <TextBlock Text="{Binding TotalScore, StringFormat={}{0:F1} 点}" FontSize="32" FontWeight="Bold" Foreground="{StaticResource SuccessBrush}" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                                </StackPanel>
                            </Border>

                            <!-- 指標詳細リスト -->
                            <StackPanel Grid.Row="2" Margin="0,0,0,20">
                                <DockPanel Margin="0,0,0,10">
                                    <TextBlock Text="Hard制約違反:" Foreground="{StaticResource TextSecondaryBrush}"/>
                                    <TextBlock DockPanel.Dock="Right" Text="{Binding HardViolationCount, StringFormat={}{0} 件}" FontWeight="Bold" Foreground="{StaticResource SuccessBrush}"/>
                                </DockPanel>

                                <DockPanel Margin="0,0,0,10">
                                    <TextBlock Text="Soft制約未達成:" Foreground="{StaticResource TextSecondaryBrush}"/>
                                    <TextBlock DockPanel.Dock="Right" Text="{Binding SoftViolationCount, StringFormat={}{0} 件}" FontWeight="Bold" Foreground="{StaticResource WarningBrush}"/>
                                </DockPanel>

                                <DockPanel Margin="0,0,0,10">
                                    <TextBlock Text="希望達成率:" Foreground="{StaticResource TextSecondaryBrush}"/>
                                    <TextBlock DockPanel.Dock="Right" Text="{Binding PersonalWishAchievementRate, StringFormat={}{0:F1} %}" FontWeight="Bold"/>
                                </DockPanel>

                                <DockPanel Margin="0,0,0,10">
                                    <TextBlock Text="夜勤回数の偏り (分散):" Foreground="{StaticResource TextSecondaryBrush}"/>
                                    <TextBlock DockPanel.Dock="Right" Text="{Binding NightDutyVariance, StringFormat={}{0:F2}}" FontWeight="Bold"/>
                                </DockPanel>

                                <DockPanel Margin="0,0,0,10">
                                    <TextBlock Text="勤務負担の偏り (分散):" Foreground="{StaticResource TextSecondaryBrush}"/>
                                    <TextBlock DockPanel.Dock="Right" Text="{Binding WorkloadVariance, StringFormat={}{0:F2}}" FontWeight="Bold"/>
                                </DockPanel>

                                <DockPanel Margin="0,0,0,4">
                                    <TextBlock Text="計算所要時間:" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12"/>
                                    <TextBlock DockPanel.Dock="Right" Text="{Binding SolverTimeSeconds, StringFormat={}{0:F1} 秒}" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                                </DockPanel>
                            </StackPanel>

                            <!-- 採用ボタン -->
                            <Button Grid.Row="3" Style="{StaticResource PrimaryButton}" Content="この案を採用する"
                                    Command="{Binding DataContext.AdoptCaseCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}"
                                    IsEnabled="{Binding IsSelected, Converter={StaticResource InverseBoolToVis}}"
                                    HorizontalAlignment="Stretch"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- ステータスバー -->
        <Border Grid.Row="2" Background="{StaticResource SurfaceBrush}" CornerRadius="6" Padding="12,8" Margin="0,16,0,0" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <TextBlock Text="{Binding StatusMessage}" FontWeight="SemiBold"/>
        </Border>
    </Grid>
</UserControl>
